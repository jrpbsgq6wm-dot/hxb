# /etc/network/interfaces -- 标准静态IP配置（无格式错误）
auto lo
iface lo inet loopback

# 有线网卡eth0 - 静态IP配置
auto eth0
iface eth0 inet static
    address 192.168.0.5
    netmask 255.255.255.0
    gateway 192.168.0.1
    dns-nameservers 192.168.0.1 8.8.8.8

