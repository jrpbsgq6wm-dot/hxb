# SVP 项目存档

本仓库中的 SVP 项目文件集中保存在此目录。

## 目录

- `源码/`：CubeSVP1500 应用工程与 Bootloader 工程。
- `协议/`：协议文档、表格和指令验证记录。
- `发行固件/`：发行固件压缩包及 SHA-256 校验值。

源码副本已排除 Keil/CubeIDE 的 `OBJ`、`Listings` 构建输出，日志、备份文件和个人 IDE 布局配置。原始 `仓库维护` 目录未被修改。工程文件和源代码保留。

## 固件包

- 文件：`发行固件/SVP_20260827.zip`
- 校验值：见 `发行固件/SHA256SUMS.txt`
